unit Click_NBIoT_config;

uses Click_NBIoT_types;

const
    _NBIOT_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _NBIOT_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _NBIOT_POLL_ENABLE : uint8_t = 1; // poll enabled
    _NBIOT_CALLBACK_ENABLE : uint8_t = 0; // calback disabled
    _NBIOT_UART_CFG : array[4] of uint32_t = (9600, _UART_8_BIT_DATA, _UART_NOPARITY, _UART_ONE_STOPBIT); 
    
end.